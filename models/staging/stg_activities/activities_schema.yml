version: 2

models:
  - name: stg_activities
    columns:
      - name: id
        tests: [not_null, unique]

      - name: type
        tests:
          - accepted_values:
              arguments:
                values: [call, email, meeting, demo, note]
              severity: warn
