version: 2

models:
  - name: stg_activities
    columns:
      - name: id
        data_tests: [not_null, unique]

      - name: type
        data_tests:
          - accepted_values:
              arguments:
                values: [call, email, meeting, demo, note]
              severity: warn
