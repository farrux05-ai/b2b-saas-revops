{
  "buildCommand": "dbt docs generate",
  "outputDirectory": "target",
  "installCommand": "pip install -r requirements.txt",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
